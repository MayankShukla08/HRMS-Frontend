// ─── Apollo Client Stub ───────────────────────────────────────────────────────
// Frontend-only demo: Apollo is not used. This stub satisfies any stale imports.

export const apolloClient = {
    mutate: async () => ({ data: null }),
    query: async () => ({ data: null }),
};
